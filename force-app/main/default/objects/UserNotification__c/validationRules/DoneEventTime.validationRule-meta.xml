<?xml version="1.0" encoding="UTF-8" ?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>DoneEventTime</fullName>
    <active>true</active>
    <description>Check that the DoneEventTime__c is not older than EventTime__c</description>
    <errorConditionFormula>DoneEventTime__c &lt;  EventTime__c</errorConditionFormula>
    <errorMessage>Done Event Time cannot be older than Event Time.
Causal loop warning: An event cannot be done before it actually happened.</errorMessage>
</ValidationRule>
